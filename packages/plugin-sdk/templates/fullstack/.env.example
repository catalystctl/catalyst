# Catalyst Plugin — Frontend Environment Configuration
# Copy this file to .env and update values as needed

# ── API Configuration ─────────────────────────────────────────────────────────

# Base URL for Catalyst API
# - Empty string: Use relative URLs (same origin)
# - Full URL: Use absolute URLs (cross-origin deployments)
VITE_API_URL=
