# Catalyst Backend Plugin Environment Configuration
# Copy this file to .env if your plugin needs environment variables

# Note: Backend plugins typically don't use Vite env vars.
# Use process.env or your runtime's equivalent for server-side config.
