# Catalyst Plugin Environment Configuration
# Copy this file to .env if your plugin needs environment variables

# Note: Minimal plugins typically don't need env vars.
# Add them here if your plugin requires runtime configuration.
