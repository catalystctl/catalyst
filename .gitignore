# Screenshot test artifacts (generated to docs/screenshots/ which IS committed)
catalyst-frontend/screenshots/
catalyst-frontend/playwright-report/
catalyst-frontend/playwright-report/
docs.md

# Local architecture/review notes (not for commit)
review-*.md
EXHAUSTIVE_REVIEW_REPORT.md

# Development-only scripts (keep these local)
catalyst-backend/generate-better-auth-schema.ts
catalyst-backend/inspect-auth.ts
catalyst-backend/update-template*.ts
catalyst-backend/upload-*.ts
catalyst-backend/sftp_host_key
catalyst-backend/schema-additions.prisma
catalyst-backend/updated-user-model.prisma

# Test environment config
tests/config.env
tests/lib/node_modules/

# Build artifacts
*.tgz
*.tar.gz
*.har
*.har.har
node_modules/

# pnpm (lockfile is committed — do NOT ignore it)

# Root environment (docker compose config)
.env

# Docker
.docker/

# IDE and OS
.vscode/
.idea/
*.swp
.DS_Store
# act (local CI testing)
.actrc
docs/testing-ci-locally.md
api-1.json
catalyst-agent/config.toml
catalyst-agent/config-e2e.toml
catalyst-agent/SECURITY_ADVISOR.md
.kilocode/setup-script
catalyst-frontend/.vite/

# Pi agent runtime state
.pi/
context.md
test-results/.last-run.json
.pi-lens
catalyst-bot/
catalyst-plugins/egg-explorer/backend/egg-index.json
catalyst-frontend/src/styles/material.css
.pi-tavily-temp/
eggs/
progress.md
packages/plugin-sdk/dist/
packages/mcp-server/dist/
.fusion.worktrees/
.fusion
.impeccable/
image.png

# Catalyst Code harness local state
.catalyst-code/

# Benchmark results (local only — not for commit)
benchmarks/
SESSION_BRIDGE_CATCODE_OPENCODE.md
.mythos.yml
.mythos/
.sphinx/
.sphinxignore
.opencode/

# Dedicated templates repo (own git project: catalystctl/catalyst-templates)
/catalyst-template/

# Plugins live in the standalone catalystctl/catalyst-plugins repo (checked out as a sibling or symlinked here for monorepo dev)
/catalyst-plugins

# Local docker backups, dumps, and announcements
catalyst-docker-backup-*/
dump.rdb
important-announcement.md

# Local security audit reports (working documents, not for the repo)
security-audit/

# Supply-chain test fixtures are generated at runtime by
# tests/28-supply-chain.test.sh (mktemp) — never commit binaries here
tests/fixtures/

# Review/handoff scratch notes (untracked working documents)
BRANCH-STACK.md

# Local agent harness state (workflows, memory, scratch — never commit)
.grok/

# LXC/Incus container artifacts (exports, local state, per-host overrides)
*.lxc
.lxc/
.incus/
lxc-data/
scripts/lxc-lab/*.local.env
