# pnpm 11+: only auth/registry settings are read from .npmrc.
# Linker, hoist, overrides, allowBuilds, and minimumReleaseAge live in
# pnpm-workspace.yaml (https://pnpm.io/settings).
#
# Kept as comments for older tooling / humans:
# node-linker=hoisted
# shamefully-hoist=true
# minimumReleaseAge=0
